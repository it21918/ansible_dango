server {
    listen 9000;
    server_name _;

    location = /favicon.ico { access_log off; log_not_found off; }
    location /static/ {
        root /home/jenkins/userSystem/base;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/jenkins/userSystem/base/base.sock;
    }
}
